export type Post = {
    postId: string,
    body: string,
    datePublished: string,
    published: boolean,
    slug: string,
    title: string,
    featuredImageUrl: string,
    abstract: string,
};
